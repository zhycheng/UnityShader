using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaseCode10 : MonoBehaviour
{
    // Start is called before the first frame update
    float startPos = 0f;
    float width = 40f;
    void Start()
    {
		//startPos += Time.deltaTime * 20;
		//if (startPos > 200f)
		//{
		//	startPos = 0f;

		//}
		//this.GetComponent<MeshRenderer>().material.SetFloat("startPos", startPos);
		//this.GetComponent<MeshRenderer>().material.SetFloat("width", width);
	}

    // Update is called once per frame
    void Update()
    {
        
	}
}
